name: Go Dependency Submission

on:
  push:
    branches: [main]
    paths:
      - go.mod
      - go.sum
      - '**.go'

permissions:
  contents: write

# Only the latest main state matters for the dependency graph, so cancel
# any in-flight submission superseded by a newer push.
concurrency:
  group: dep-submission-${{ github.ref }}
  cancel-in-progress: true

jobs:
  dependency-submission:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Go Environment
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.26.5'
          cache: true
          cache-dependency-path: go.sum

      - name: Submit Dependencies
        uses: actions/go-dependency-submission@2ca822d507c895fa434a7fe69cacd1cfe9b0aa45 # Node 24-compatible action revision
        with:
          go-mod-path: go.mod
          go-build-target: cmd/frame-tv-art-manager/main.go
