--- internal/optimize/resize.go
+++ internal/optimize/resize.go
@@ -688,27 +688,27 @@
					b += noise

					// Faster clamp and cast without math.Max/Min
-					if r < 0 {
+					switch {
+					case r < 0:
						src.Pix[i] = 0
-					} else if r > 255 {
+					case r > 255:
						src.Pix[i] = 255
-					} else {
+					default:
						src.Pix[i] = uint8(r)
					}

-					if g < 0 {
+					switch {
+					case g < 0:
						src.Pix[i+1] = 0
-					} else if g > 255 {
+					case g > 255:
						src.Pix[i+1] = 255
-					} else {
+					default:
						src.Pix[i+1] = uint8(g)
					}

-					if b < 0 {
+					switch {
+					case b < 0:
						src.Pix[i+2] = 0
-					} else if b > 255 {
+					case b > 255:
						src.Pix[i+2] = 255
-					} else {
+					default:
						src.Pix[i+2] = uint8(b)
					}
				}
			}
