1. Consolidate `client_metadata.go`, `client_slideshow.go`, and `client_upload.go` into `client_art.go`.
2. Delete the now-empty files (`client_metadata.go`, `client_slideshow.go`, `client_upload.go`).
3. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
4. Submit PR.
